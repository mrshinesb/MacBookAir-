1.
Open your terminal:

2.
cd /Users/sbglobal/development/f1
git init

3. 
git add .
git commit -m "Initial Flutter project commit"

Step 4: Link to Your GitHub Repository
Replace <your-username> and <repo-name> in the command below:

git remote add origin https://github.com/<your-username>/<repo-name>.git
Example:

git remote add origin https://github.com/sbglobal/f1.git
5.
git branch -M main
git push -u origin main
6.
 (Optional) Add .gitignore for Flutter
Before pushing, create a .gitignore file if you don’t have one:

touch .gitignore

build/
.dart_tool/
.packages
.pub-cache/
.flutter-plugins
.flutter-plugins-dependencies
.idea/
android/local.properties
*.iml
.env

git add .gitignore
git commit -m "Add Flutter .gitignore"
git push

